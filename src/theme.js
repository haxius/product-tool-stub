export const globalStyles = `
  html,body,#root {
    margin: 0;
    padding: 0;
    border: 0;
  }
`;

const Theme = {
  renderer: {
    maxWidth: "900px",
    maxHeight: "900px"
  },
  view: {
    minWidth: "320px",
    maxWidth: "900px"
  }
};

export default Theme;
